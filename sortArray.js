let numbers = [24, 65, 21, 5, 9, 32, 42, 80, 57];
let names = ["Zenvo", "Erica", "Jordie", "Alicia", "Rendon"];

let merge = numbers.concat(names);
console.log(merge);

let numrev = numbers.reverse();
console.log(numrev);

let nameSorted = names.sort();
console.log(nameSorted);
